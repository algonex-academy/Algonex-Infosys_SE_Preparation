SEARCHING PATTERN
Given some data - determine whether a target exists- and possibly find its position/value

1. Linear Search —Foundation telsukondii
Check every element one by one until you find the target

arr = [10, 5, 20, 8, 15]
target = 20

for i in range(len(arr)):            index:   0   1   2   3   4
    if arr[i] == target:             arr =   10   5  20   8  15
        print(i) # 2                                  |
                                                    Target
##Oka Pattern chudandii so don't memorize the code just Understand the thought process:
               I have a target with input
                       |
                Start from the beginning
                       |
                Compare current element with target
                       |
                Match?
                 ┌─────┴─────┐
                YES          NO            #Ilaa alochinchandi searching problemski
                 |            |
                Return       Continue
                              |
                          Next element

╰┈➤ What If Target Doesn't Exist?
Example: arr = [10, 5, 20, 8, 15]
target = 100
found = False
for i in range(len(arr)):
    if arr[i] == target:
        found = True                      Linear Search
        break                             Best:   O(1)
if found:                                 Worst:  O(n)
    print("Found")                        Space:  O(1)
else:
    print("Not Found")

╰┈➤ Searching By Value vs Searching By Index
#Search value
for x in arr: # Ikkada x anedi value
    if x == target:
        print("Found")

#Search index
for i in range(len(arr)):
    if arr[i] == target:
        print(i) # Ikkada i is the position

╰┈➤Pattern: Finding occurences with (𝗗𝘂𝗽𝗹𝗶𝗰𝗮𝘁𝗲 𝗩𝗮𝗹𝘂𝗲𝘀)
arr = [10, 20, 5, 20, 8, 20]
target = 20

#first occurrence:
for i in range(len(arr)):
    if arr[i] == target:
        print(i)
        break  #1

#all occurrences:
for i in range(len(arr)):
    if arr[i] == target:
        print(i) # 1 3 5 -line by line
=> Ikkada The algorithm depends on exactly what the problem asks


# Last Occurrence of an element in a list
D = [12,3,324,23,42,3,213,23]
T = 23
count =0
for i in range(len(D)):
          if D[i] == T:
                    count += 1
print(f"{D[i]} at {i}") #23 at 7
print(f"Total occurrences: {count}")

╰┈➤𝗦𝗲𝗮𝗿𝗰𝗵𝗶𝗻𝗴 𝗣𝗮𝘁𝘁𝗲𝗿𝗻 #𝟭
for i in range(len(arr)):   #Pattern: Traverse -> Check condition -> Match? -> Perform required action
    if arr[i] == target:
        # found 

Note: The condition doesn't always have to be: arr[i] == target
For exampleki : Find whether any number is greater than 50
for x in arr:
    if x > 50:
        print("Found")
        break #Now you're still doing searching but with conditiontho not an exact value

Find whether 20 exists
Find whether any number is greater than 20(x > 20)
Find whether any even number exists(x % 2 == 0)
Find the first negative number(x < 0)

## Saree ee prblem cheyy ippudee
arr = [12, 7, 25, 4, 18, 30, 9]
target = 18 #Print Dongaa otherwise Dhora
╰┈➤Modify it to print the index of the target

╰┈➤Searching Pattern 2 — First & Last Occurrence
-- Linear search antee traverse and check a condition - Ippudu Don't just find whether the target exists - Track where it occurs
       0   1   2   3   4   5   6
arr = [10, 20, 5, 20, 8, 20, 15]
            1      3      5
target = 20 [ First occurrence = 1 amd Last occurrence  = 5 ]

#1. Finding the First Occurrence
arr = [10, 20, 5, 20, 8, 20, 15]                    #First (break)
target = 20                                         answer = -1
for i in range(len(arr)):                           for i in range(len(arr)):
    if arr[i] == target:                               if arr[i] == target:
        print(i)                                            answer = i
         break #Ikkada pattern: Find -> immediately stop     break                                               break

#2. Finding the Last Occurrence(We cannot stop at the first match) - we find the target, we update our answer
arr = [10, 20, 5, 20, 8, 20, 15]
target = 20
last = -1                                                #Last (Don't break)
for i in range(len(arr)):                                answer = -1
    if arr[i] == target:                                 for i in range(len(arr)):
        last = i                                         if arr[i] == target:
print(last) #Pattern chduu: Find -> remember -> continue       answer = i

╰┈➤Ikakda last = -1 ni sentinel value antaam used when Target was not found meeru coding roundslo chudochu

╰┈➤Now Count Occurrences
arr = [10, 20, 5, 20, 8, 20, 15]
target = 20
count = 0
for x in arr:
    if x == target:
        count += 1
print(count)

Pattern 1 — Existence
Pattern 2 — Position
Pattern 3 — Frequency

Your Next Challenge in Algonex Buddy Pattern
-> Don't code immediately -First understand the requirement
arr = [4, 7, 2, 7, 9, 7, 3, 7]
target = 7

mundhu find all three:
1. Does 7 exist?
2. What is the first index?
3. What is the last index?
4. How many times does 7 occur?

Expected answers enti:
exists = ?
first = ?
last = ?       --> Write one single for loop that calculates all four
count = ? 

Restrictions:
-> One loop, if, variables, break only if genuinely necessary
-> No in, No .index(), No .count()
##This is where we start combining patterns rather than solving isolated questions ardamindaa

╰┈➤Searching Pattern 3 — Binary Search
#Linear Search Works on: [10, 5, 20, 8, 15]
We can check: 10 -> 5 -> 20 -> 8 -> 15 every element -> Time: O(n)

#Binary Search - Requires a sorted collection
arr = [2, 5, 8, 10, 15, 20, 25, 30]  -> Instead of checking every element- we repeatedly cut the search space in half ->That's the entire idea anthee

##╰┈➤The Core Idea chudhaama (start -> middle -> compare)
arr = [2, 5, 8, 10, 15, 20, 25, 30]
target = 25

#Start:
left                    right
 |                        |
[2, 5, 8, 10, 15, 20, 25, 30]

#Find the middle:
middle = (left + right) // 2 #10

#Compare: 10 < 25

-> Because the array is sorted, we immediately know: Everything to the left of 10 is also too small
So discard half: [10, 15, 20, 25, 30]
                  |                |
                left            right

#Again find middle: 20 and Compare: 20 < 25 ->Discard everything before it
-> Now: [25, 30] and Middle: 25 ->Found kadaa

╰┈➤Ikkada The Three Decisions anthee
Every iteration asks exactly one question: arr[mid] == target
If yes: FOUND

If: arr[mid] < target  ->Target must be on the right: left = mid + 1
If: arr[mid] > target  ->Target must be on the left: right = mid - 1 ##Antee idee binary search

╰┈➤Basic code ownga try cheyandii
arr = [2, 5, 8, 10, 15, 20, 25, 30]
target = 25

left = 0
right = len(arr) - 1

while left <= right:
    mid = (left + right) // 2
    if arr[mid] == target:
        print("Found")
        break
    elif arr[mid] < target:
        left = mid + 1
    else:
        right = mid - 1 # Found

╰┈➤Enduku Binary Search useful
Linear Search  -> O(n)
Binary Search  -> O(log n) #Ee log n pattern is one of the most important concepts in DS gurthupetkondii
